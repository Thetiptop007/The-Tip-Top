export const featureFlags = {
  // Core Features (Always Enabled)
  dashboard: true,
  allOrders: true,
  
  // Advanced Features (Can be enabled later)
  analytics: false,
  aiInsights: false,
  departmentRouting: false,
  predictiveMaintenance: false,
};

export default featureFlags;
